'use strict';
// the module can later be imported like this by a .js file, but only if it's evaluated as a module!
import * as tic from 'tic';

tic.print('Hello!', 34, 96, 12, true, 2, true);
tic.reset(3, 4);
